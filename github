#!/bin/bash

# Display GitHub account creation instructions
echo "1. Go to GitHub (https://github.com) and create an account if you don't have one."
echo "2. After creating an account, log in to your GitHub account."

# Prompt the user to add the SSH key
read -p "3. Go to 'Settings' > 'SSH and GPG keys' in your GitHub account settings. Press Enter when ready."

# Display instructions for adding the SSH key
echo "4. Click 'New SSH key' or 'Add SSH key.'"
echo "5. Enter a title for the key (e.g., 'My WSL SSH Key')."
echo "6. In the 'Key' field, paste the following SSH key (public key):"
echo
cat ~/.ssh/id_ed25519.pub
echo
echo "7. Click 'Add SSH key' or 'Save' to add the key to your GitHub account."

# End of instructions
echo "You've successfully added your SSH key to your GitHub account. You can now use it for Git operations."

